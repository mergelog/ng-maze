import { TestBed } from '@angular/core/testing';
import { DialogTargetComponent } from './dialog-target.component';

export function createForTest(): unknown {
  return TestBed.createComponent(DialogTargetComponent);
}
