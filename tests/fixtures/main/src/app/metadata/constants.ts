import { LeafComponent } from '../basic/leaf.component';

export const METADATA_SELECTOR = 'metadata-const-host';
export const SELECTORS = { property: 'metadata-property-host' } as const;
export const SHARED_IMPORTS = [LeafComponent];
